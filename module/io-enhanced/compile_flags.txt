# Fallback for clangd if build/compile_commands.json is missing or stale.
# Standalone layout: use dap-sdk/module/core/include for dap_common.h.
# After `cmake -B build -S .` use compile_commands.json in build/ (see .clangd).
-std=gnu11
-xc
-D_GNU_SOURCE
-DDAP_IO_STATS
-I
include
-I
../core/include
