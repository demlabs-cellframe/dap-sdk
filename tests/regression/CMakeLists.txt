# =======================================================================================
# DAP SDK Regression Tests
# Tests that reproduce known bugs and should FAIL until the bug is fixed
# =======================================================================================

message("[*] DAP SDK Regression Tests")

# Add subdirectories
add_subdirectory(io)
add_subdirectory(net)
