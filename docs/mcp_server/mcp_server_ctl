#!/bin/bash
# Скрипт для запуска MCP сервера DAP SDK в фоновом режиме

# Цвета для вывода
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Пути
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SERVER_SCRIPT="$SCRIPT_DIR/run_server.py"
PID_FILE="$SCRIPT_DIR/mcp_server.pid"
LOG_FILE="$SCRIPT_DIR/mcp_server.log"
PYTHONPATH="/home/naeper/.local/share/pipx/venvs/mcp/lib/python3.13/site-packages:$SCRIPT_DIR"

# Функции
show_usage() {
    echo -e "${BLUE}Использование:${NC}"
    echo "  $0 start [path]  - Запустить сервер (опционально с путем к проекту)"
    echo "  $0 stop          - Остановить сервер"
    echo "  $0 restart [path]- Перезапустить сервер"
    echo "  $0 status        - Проверить статус сервера"
    echo "  $0 logs          - Показать логи сервера"
    echo "  $0 test          - Запустить тесты"
    echo ""
    echo -e "${BLUE}Примеры:${NC}"
    echo "  $0 start"
    echo "  $0 start /path/to/dap-sdk"
    echo "  $0 status"
    echo "  $0 logs"
}

is_server_running() {
    if [ -f "$PID_FILE" ]; then
        local pid=$(cat "$PID_FILE")
        if ps -p "$pid" > /dev/null 2>&1; then
            return 0
        else
            # PID файл существует, но процесс не запущен
            rm -f "$PID_FILE"
            return 1
        fi
    fi
    return 1
}

start_server() {
    local project_path="${1:-/home/naeper/work/cellframe-node/dap-sdk}"
    
    if is_server_running; then
        echo -e "${YELLOW}⚠️  Сервер уже запущен (PID: $(cat "$PID_FILE"))${NC}"
        return 1
    fi

    echo -e "${BLUE}🚀 Запуск MCP сервера DAP SDK...${NC}"
    echo -e "${BLUE}📁 Проект: ${project_path}${NC}"
    echo -e "${BLUE}📝 Логи: ${LOG_FILE}${NC}"

    # Проверяем существование проекта
    if [ ! -d "$project_path" ]; then
        echo -e "${RED}❌ Ошибка: Директория проекта не найдена: $project_path${NC}"
        return 1
    fi

    # Запускаем сервер в фоне через демон
    cd "$SCRIPT_DIR"
    PYTHONPATH="$PYTHONPATH" nohup python3 run_server_daemon.py "$project_path" > "$LOG_FILE" 2>&1 &
    local server_pid=$!
    
    # Сохраняем PID
    echo "$server_pid" > "$PID_FILE"
    
    # Ждем немного чтобы проверить что сервер запустился
    sleep 3
    
    # Проверяем что процесс все еще работает
    if ps -p "$server_pid" > /dev/null 2>&1; then
        echo -e "${GREEN}✅ Сервер успешно запущен (PID: $server_pid)${NC}"
        echo -e "${GREEN}📊 Проверить статус: $0 status${NC}"
        echo -e "${GREEN}📋 Посмотреть логи: $0 logs${NC}"
        return 0
    else
        echo -e "${RED}❌ Процесс сервера завершился${NC}"
        echo -e "${RED}📋 Проверьте логи: $0 logs${NC}"
        rm -f "$PID_FILE"
        return 1
    fi
}

stop_server() {
    if ! is_server_running; then
        echo -e "${YELLOW}⚠️  Сервер не запущен${NC}"
        return 1
    fi

    local pid=$(cat "$PID_FILE")
    echo -e "${BLUE}🛑 Остановка MCP сервера (PID: $pid)...${NC}"
    
    kill "$pid"
    
    # Ждем завершения
    local count=0
    while ps -p "$pid" > /dev/null 2>&1 && [ $count -lt 10 ]; do
        sleep 1
        ((count++))
    done
    
    if ps -p "$pid" > /dev/null 2>&1; then
        echo -e "${YELLOW}⚠️  Принудительная остановка...${NC}"
        kill -9 "$pid"
        sleep 1
    fi
    
    rm -f "$PID_FILE"
    echo -e "${GREEN}✅ Сервер остановлен${NC}"
}

restart_server() {
    local project_path="$1"
    echo -e "${BLUE}🔄 Перезапуск MCP сервера...${NC}"
    stop_server
    sleep 1
    start_server "$project_path"
}

show_status() {
    if is_server_running; then
        local pid=$(cat "$PID_FILE")
        local uptime=$(ps -o etime= -p "$pid" | tr -d ' ')
        echo -e "${GREEN}✅ Сервер запущен${NC}"
        echo -e "${GREEN}🆔 PID: $pid${NC}"
        echo -e "${GREEN}⏱️  Время работы: $uptime${NC}"
        echo -e "${GREEN}📝 Логи: $LOG_FILE${NC}"
        
        # Показываем последние строки логов
        if [ -f "$LOG_FILE" ]; then
            echo -e "\n${BLUE}📋 Последние логи:${NC}"
            tail -5 "$LOG_FILE" | while read line; do
                echo -e "${BLUE}  $line${NC}"
            done
        fi
    else
        echo -e "${RED}❌ Сервер не запущен${NC}"
        return 1
    fi
}

show_logs() {
    if [ -f "$LOG_FILE" ]; then
        echo -e "${BLUE}📋 Логи MCP сервера:${NC}"
        echo -e "${BLUE}📁 Файл: $LOG_FILE${NC}"
        echo -e "${BLUE}============================================================${NC}"
        tail -f "$LOG_FILE"
    else
        echo -e "${YELLOW}⚠️  Файл логов не найден: $LOG_FILE${NC}"
        return 1
    fi
}

run_tests() {
    echo -e "${BLUE}🧪 Запуск тестов MCP сервера...${NC}"
    cd "$SCRIPT_DIR"
    
    if command -v pytest &> /dev/null; then
        PYTHONPATH="$PYTHONPATH" pytest tests/ -v
    else
        echo -e "${RED}❌ pytest не установлен${NC}"
        echo -e "${YELLOW}💡 Установите: pip install pytest pytest-asyncio${NC}"
        return 1
    fi
}

# Основная логика
case "${1:-}" in
    start)
        start_server "$2"
        ;;
    stop)
        stop_server
        ;;
    restart)
        restart_server "$2"
        ;;
    status)
        show_status
        ;;
    logs)
        show_logs
        ;;
    test)
        run_tests
        ;;
    *)
        show_usage
        exit 1
        ;;
esac
