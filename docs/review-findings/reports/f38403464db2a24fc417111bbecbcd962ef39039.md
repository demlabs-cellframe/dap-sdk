# Review Report: `f38403464db2a24fc417111bbecbcd962ef39039`

Date: 2026-04-21

Scope: full commit review (logic/code-quality/regression-risk) + targeted UDP/stream validation.

## Reviewed changes
- `net/trans/udp/dap_net_trans_udp_server.c`

## Findings
No findings for this commit.

## Analysis notes
- `s_flow_ctrl_packet_parse_cb()`:
  - packet type is now encoded into per-packet delivery buffer format `[type][payload]`;
  - this removes receive-side coupling through session-global `last_recv_type` and avoids cross-packet type races in callbacks.
- ACK/control packets without payload:
  - parser now returns `payload_size = 0`, `payload = NULL`, `private_ctx = NULL` and frees temporary decrypted buffer immediately;
  - this fixes the no-payload parse leak path where Flow Control skipped delivery/free branches.
- `s_flow_ctrl_payload_deliver_cb()`:
  - type extraction is now local (`a_payload[0]`), payload passed to handlers remains raw transport data (`a_payload + 1`);
  - dispatch behavior for `SESSION_CREATE` / `DATA` / `KEEPALIVE` / `CLOSE` remains consistent with existing handler contracts.
- No stubs/placeholders or ad-hoc bypasses introduced in production path by this commit.

## Validation
- Built targets (`build-ci-clang-tests`):
  - `test_trans_udp`
  - `test_trans_dns`
  - `test_stream`
  - `test_stream_trans`
  - `test_stream_trans_full`
  - `test_stream_obfuscation`
- Executed tests:
  - `test_trans_udp` - PASSED
  - `test_trans_dns` - PASSED
  - `test_stream` - PASSED
  - `test_stream_trans` - PASSED
  - `test_stream_trans_full` - PASSED
  - `test_stream_obfuscation` - PASSED
- Not executed by explicit request:
  - `test_udp_multiclient_regression`

## Conclusion
Commit is clean in reviewed scope; no regressions detected.
